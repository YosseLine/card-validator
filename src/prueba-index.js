window.addEventListener('load', () =>{

function validateName (id) {
  for(let user in data ){
    if(user === 'user') {
      for (let eachName in data) {
        console.log(user);
      }
    }
  };
}
});
